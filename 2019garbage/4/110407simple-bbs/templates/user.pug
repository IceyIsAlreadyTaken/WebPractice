extends layout.pug

block content 
  if user
    h2 用户
    div= user.name

    div
      h2 他发过的帖子
      ul
        each post in posts
          li
            a(href="/post/" + post.id)= post.content
            | 
            a(href="/delete-post/" + post.id) 删除
      h2 他发表的评论
      ul
        each comment in comments
          li 
            h5
            span= comment.postTitle
            | 
            a(href="/delete-comment/" + comment.id) 删除
            p= comment.content
  else
    |没有您要找的用户         